PORT=3001

# Servidor SQL compartido (app + catálogo Contpaq)
DB_HOST=localhost
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=SqlGlobal_Dev1!

# BD principal (Flotilla / Ventas)
DB_NAME=flotilla
DB_SYNC=true

# BD catálogo Contpaq (clientes / productos) — mismo acceso, otro nombre
DB_CATALOG_NAME=PROMAC
DB_CATALOG_REQUIRED=false

JWT_SECRET=flotilla-dev-jwt-secret
JWT_EXPIRES_IN=8h

CORS_ORIGINS=http://localhost:4200,*.vercel.app

ADMIN_NOMBRE=Administrador
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Se20sepaad80$
